# Build / install artifacts
build/
install/
log/

# Session-specific data (landmarks + rtabmap databases)
# These are generated by running the Phase 1 mapping pipeline on recorded
# rosbags; they are not part of the package source.
landmarks/
rtabmap_db/
